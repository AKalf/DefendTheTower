using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TowerStatsScript : MonoBehaviour
{
    //[SerializeField]
    // int maxHealth
    // int cuurentHealth
    // int currentLevel
    // int maxLevel
    // AudioSource

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    // ChangeHealthByAmount (int amount)
        // health += amount
        // check if health <= 0 and to call EndGame function (step for future)
        // call U.I manager to change the GUI with the new health of the tower (when we have GUI)

    // GetCurrentHealth

    // LevelUp (int levelToGo)
        // TO-DO: change models, stats, etc (step for future)
}
